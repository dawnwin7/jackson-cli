{
  "name": "jackson-cli-monorepo",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24.18"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "smoke": "turbo build && bash scripts/smoke.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "turbo": "2.10.8"
  }
}
